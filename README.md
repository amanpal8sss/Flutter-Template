--clone the repo
  git clone https://github.com/amanpal8sss/Flutter-Template.git


  --flutter clean and pub get
  #flutter clean
  #flutter pub get

  -- Run the Project
  flutter run --release -d chrome --web-port 3000 --web-browser-flag "--disable-web-security" --dart-define=APP_ENV=local
  
